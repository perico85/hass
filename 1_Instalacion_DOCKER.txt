#1_INICIAR CON CABLE DE RED CONECTADO
sudo apt update && upgrade \
sudo apt-get install \
jq \
wget \
curl \
udisks2 \
libglib2.0-bin \
network-manager \
apparmor \
apt-transport-https \
ca-certificates \
dbus -y

#2
curl -fsSL https://get.docker.com -o get-docker.sh

#3
sh get-docker.sh

#4
sudo usermod -aG docker $USER